*** Variables ***
# Number of retries (Numero de reintentos)
${SMALL_RETRY_COUNT}           2x
${MEDIUM_RETRY_COUNT}          3x
${LARGE_RETRY_COUNT}           5x

# Delay for a retry (Retraso para un reintento)
${RETRY_DELAY}                 1 s

# User credentials
${USERNAME}                    jruiz@q4tech.com
${PASSWORD}                    1

# Application constants
${BROWSER_TAB_TITLE}           Close-Up Call